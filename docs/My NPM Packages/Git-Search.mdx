---
title: Git Search
id: git-search
slug: /npm/git-search
---

import GitImageNpm from "@site/src/assets/git-search-npm.png";

Are you tired of manually searching through Github to find the repositories you need? Well, have no
fear because [Git-Search](https://www.npmjs.com/package/git-search-repos) is here!

[Git-Search](https://www.npmjs.com/package/git-search-repos) is a powerful tool that returns a list
of Github repositories sorted in descending order based on their last updated time. And the best
part? It was built by me, with love and care put into every line of code. 💖

<img src={GitImageNpm} width="800rem" style={{ borderRadius: "12px", marginBottom: "1rem" }} />

To get started with [Git-Search](https://www.npmjs.com/package/git-search-repos), simply install it
using NPM or Yarn, following the instructions provided in the data above. You can then use the code
snippets mentioned above to easily access and use
[Git-Search](https://www.npmjs.com/package/git-search-repos) in your projects.

One of the coolest things about [Git-Search](https://www.npmjs.com/package/git-search-repos) is that
it allows you to specify the Github username of the person whose repositories you want to search
for. And if you don't specify the page and per_page values,
[Git-Search](https://www.npmjs.com/package/git-search-repos) will use the default values of 1 and 30
respectively.

So, if you're tired of spending hours sifting through Github repositories and want a faster, more
efficient way to search for repositories,
[Git-Search](https://www.npmjs.com/package/git-search-repos) is the tool for you. And since it was
built by me, you know it's top-notch quality.

Give [Git-Search](https://www.npmjs.com/package/git-search-repos) a try and see for yourself just
how much easier it can make your coding life. And if you find it useful, be sure to share it with
your fellow developers so we can all benefit from the power of
